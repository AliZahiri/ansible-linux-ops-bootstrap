# Add Node Exporter alert planning notes

<!-- daily-pr-task: node-exporter-alerts -->

Node Exporter metrics become more useful when paired with baseline alerts.

Starter alert areas:

- disk space and inode pressure
- high load average
- memory pressure
- network saturation
- filesystem read-only state
- systemd service failures where collected separately

## Portfolio Value

Shows that monitoring installation is tied to actionable operations.

## Validation

Review the markdown file and confirm alert areas are host-level and actionable.
